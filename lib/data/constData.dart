import 'package:flutter/material.dart';

const primaryBackground = Color(0xfffef4ad);
const secondaryGoldColor = Color(0xffdfad00);
const thirdRedColor = Color(0xffb91e1a);

const primaryBackgroud2RoyalBlue = Color(0xff010954);
const secondaryBackgroundRoyalBlueLess = Color(0xff020973);

const amberColor = Colors.amber;
Color amberWithOpacity = Colors.amber.shade300;

BoxShadow boxShadow = BoxShadow(
  offset: const Offset(0, 4),
  blurRadius: 5,
  spreadRadius: 0,
  color: Colors.black.withOpacity(0.5),
);
BoxShadow boxShadowLow = BoxShadow(
  offset: const Offset(0, 2),
  blurRadius: 5,
  spreadRadius: 0,
  color: Colors.black.withOpacity(0.7),
);
